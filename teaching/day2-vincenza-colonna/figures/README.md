# Figures

Place lecture images in this directory and reference them with relative paths, for example:

```markdown
![Short reads and a structural variant](figures/read_length_vs_variant_type.png)
```

Prefer descriptive lowercase filenames with underscores. Keep editable source files when practical, and document external image sources and licenses in the handout.
